#include <stdlib.h>
#include <stdio.h>

int main(){
    int a, b, count[10], d;

    for(a = 0; a < 9; a++){
        for (b = a + 1; b < 10; b++){
            count[a];
            printf("%d\n", count[a]);
        }
        // printf("%d\n", a);
    }   

    return 0;
}